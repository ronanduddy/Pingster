<?php

/**
 * These are, strictly speaking, AWS-S3 settings, but are
 * not actually settings the plugin expects, instead being
 * passed from the Pingster objects.
 */
$config['Pingster']['s3_bucket'] = 'pingster';
$config['Pingster']['s3_region'] = 'REGION_IRELAND';
